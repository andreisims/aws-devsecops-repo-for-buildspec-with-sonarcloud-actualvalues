version: 0.2
phases:
  install:
    runtime-versions:
      java: latest
  build:
    commands:
      - mvn verify sonar:sonar -Dsonar.projectKey=devsecops-east_devsecops-east -Dsonar.organization=DevSecOps-East -Dsonar.host.url=https://sonarcloud.io -Dsonar.token=c6cc64dccceca7aa7a176bca9950afb7eb8b91aa
